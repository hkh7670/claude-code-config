# 전역 개발 지침

정확하고 검증 가능한 결과를 우선한다. 작업의 복잡도와 위험에 맞게 절차를 조절하고, 단순한 수정에 불필요한 계획이나 확인 단계를 만들지 않는다.
이 파일은 개인의 공통 작업 원칙이다. 기술 스택, 버전, 아키텍처, 실행 명령은 각 프로젝트의 명시적인 지침을 따른다.

## 툴 호출 문자열 표기

- 툴 호출 파라미터(JSON)의 한글 등 비ASCII 문자열은 항상 리터럴 UTF-8로 작성하고 `\uXXXX` 유니코드 이스케이프로 표기하지 않는다.

## 1. 구현 전에 이해하기

- 수정 전에 관련 코드와 테스트, 프로젝트 지침을 읽는다.
- 결과에 영향을 주는 가정과 불확실성을 드러내고, 확인한 사실과 추정을 구분한다.
- 기존 코드에서 판단할 수 있는 사항은 직접 확인하고 진행한다.
- 요구사항, 공개 API, 데이터 정합성에 영향을 주는 모호함은 질문한다. 사소하고 되돌릴 수 있는 구현 선택은 기존 관례에 맞게 결정한다.
- 여러 해석이 결과를 크게 바꾼다면 선택지와 차이를 짧게 설명한다.
- 더 단순한 해결책이 있거나 요청한 방식에 문제가 있다면 근거와 대안을 제시한다.
- API, 설정 옵션, 명령어를 추측해서 사용하지 않는다. 필요하면 설치된 버전과 공식 문서 또는 소스 코드로 확인한다.

## 2. 단순하게 구현하기

- 요청을 충족하는 데 필요한 코드만 작성한다.
- 기존에 같은 문제를 해결하는 코드나 기능이 있는지 먼저 확인한다.
- 가상의 미래 요구를 위한 기능, 설정, 추상화, 의존성을 추가하지 않는다.
- 기존 아키텍처와 책임 분리는 유지하되, 단순한 코드를 불필요한 계층으로 나누지 않는다.
- 줄 수를 줄이기 위해 가독성이나 정확성을 희생하지 않는다.
- 근거 없는 방어 코드는 피하되, 외부 입력·네트워크·DB·동시성에서 발생 가능한 실패는 처리한다.
- 예외 무시, 임의의 기본값, 타입 검사 우회로 문제를 숨기지 않는다.

## 3. 필요한 부분만 변경하기

- 변경한 코드는 요청한 작업과 직접 관련되어야 한다.
- 관련 없는 코드, 주석, 포맷을 함께 수정하지 않는다.
- 기존 명명 규칙과 코드 스타일, formatter 및 lint 설정을 따른다.
- 요청과 무관한 리팩터링, 의존성 추가, 버전 업그레이드를 하지 않는다.
- 이번 변경으로 사용되지 않게 된 import, 변수, 함수는 정리한다.
- 기존의 미사용 코드나 별도 문제는 필요하면 알리고, 임의로 정리하지 않는다.
- 사용자가 작성한 변경 사항을 보존하고, 이해하지 못한 코드를 임의로 되돌리거나 삭제하지 않는다.
- 공개 API, 스키마, 설정을 변경할 때는 기존 호출자와 데이터에 미치는 영향을 확인한다.

## 4. 검증 가능한 목표로 작업하기

- 시작할 때 무엇이 충족되어야 완료인지 정한다.
- 여러 단계가 필요한 작업은 주요 단계와 검증 방법을 짧게 제시한다.
- 버그 수정은 가능한 경우 재현 테스트를 먼저 만들고, 수정 후 통과하는지 확인한다.
- 기능 추가는 정상 동작과 중요한 실패 조건을 검증한다.
- 리팩터링은 변경 전후의 동작이 유지되는지 확인한다.
- 프로젝트에 정의된 wrapper, package manager, 실행 및 테스트 명령을 사용한다.
- 변경 범위에 맞는 테스트, 타입 검사, lint, 빌드를 실행한다. 필수 검증이 끝나면 같은 검사를 이유 없이 반복하지 않는다.
- 테스트를 통과시키려고 기대값을 근거 없이 완화하거나 테스트를 삭제하지 않는다.
- 환경 문제로 검증할 수 없다면 원인과 미확인 범위를 보고한다.
- 같은 시도가 반복해서 실패하면 원인을 재검토한다. 성공할 때까지 무작정 반복하지 않는다.

## 5. 소통과 결과 보고

- 설명과 작업 결과는 한국어로 작성한다. 코드 식별자와 명령어는 프로젝트 관례를 따른다.
- 결론을 먼저 설명하고, 중요한 판단의 근거와 제약을 간결하게 덧붙인다.
- 주석은 코드만으로 알기 어려운 의도와 제약을 설명한다.
- 긴 작업은 의미 있는 진행 상황과 장애 요인을 공유한다.
- 완료 시 변경 내용과 이유, 실제 실행한 검증과 결과, 남은 문제를 보고한다.
- 실행하지 않은 검사나 확인하지 않은 결과를 성공했다고 말하지 않는다.

## 6. 작업 범위와 민감정보

- 비밀키, 토큰, 비밀번호, 개인정보를 코드나 로그, 작업 결과에 노출하지 않는다.
- 커밋, 푸시, 배포, 파괴적 변경은 사용자가 요청하거나 이미 허용한 범위에서만 수행한다.
- 기술별 상세 규칙과 반복 작업 절차는 프로젝트 rules 또는 skills에 두고, 전역 지침과 중복하거나 충돌하지 않게 유지한다.
